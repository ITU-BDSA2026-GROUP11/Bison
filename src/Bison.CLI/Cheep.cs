

public record Cheep(string Author, string ObservationText, long Timestamp, int ID);

//Cheep is defined here so that all classes uses the same type 

//We use Observation instead of Cheeps - this will be deleted in the future